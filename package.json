{
  "name": "kitto-openui",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "generate:openui-spec": "node scripts/generate-openui-spec.mjs",
    "build:standalone-player": "npm run build:standalone-player --workspace frontend && node scripts/embed-standalone-player-assets.mjs",
    "dev:standalone-player": "concurrently -k -n standalone-build,standalone-embed -c blue,blue \"npm run build:standalone-player:watch --workspace frontend\" \"node scripts/embed-standalone-player-assets.mjs --watch\"",
    "dev": "npm run generate:openui-spec && concurrently -n frontend,backend,standalone -c cyan,magenta,blue \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\" \"npm run dev:standalone-player\"",
    "build": "npm run generate:openui-spec && npm run build:standalone-player && npm run build --workspaces",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend",
    "test": "npm run test:frontend && npm run test:backend",
    "test:frontend": "npm run test --workspace frontend",
    "test:backend": "npm run test --workspace backend",
    "start": "npm run start --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
